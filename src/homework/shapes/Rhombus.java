package homework.shapes;

import homework.Color;

public class Rhombus extends Shape {
    public Rhombus(double pointX, double pointY, String shapeName, Color shapeColor) {
        super(pointX, pointY, shapeName, shapeColor);
    }
}
