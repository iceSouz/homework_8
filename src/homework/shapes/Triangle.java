package homework.shapes;

import homework.Color;
import homework.shapes.Shape;

public class Triangle extends Shape {
    public Triangle(double pointX, double pointY, String shapeName, Color shapeColor) {
        super(pointX, pointY, shapeName, shapeColor);
    }
}
